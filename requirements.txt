# Python dependencies for map.py and cv.py
# Windows Python 3.12+ uses Dear ImGui Bundle because the legacy pyimgui
# package does not publish CPython 3.14 wheels.
# Linux/WSLg system dependency: python3-tk (install with apt, not pip)
glfw>=2.8
imgui[glfw]>=2.0; python_version < "3.12"
imgui-bundle>=1.92.801,<1.93; python_version >= "3.12"
PyOpenGL>=3.1
numpy>=1.24
opencv-python>=4.8
pyserial>=3.5
dynamixel-sdk>=3.7
openpyxl>=3.1
